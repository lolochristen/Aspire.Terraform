resource "azurerm_key_vault_secret" "{{name}}" {
  name         = "{{Resource.SecretName}}"
  value        = "{{value}}"
  key_vault_id = azurerm_key_vault.{{parent.name}}.id
}

locals {
  {{name}} = {
    key_vault_secret_id = azurerm_key_vault_secret.{{name}}.versionless_id
  }
}

