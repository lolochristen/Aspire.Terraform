# hi {{name}}

locals {
  {{name}} = {
    output1 = "TEST_OUTPUT_1"
    output2 = "{{Parameters.tfp1}}"
    connectionString = "Server={{Parameters.tfp1}}"
    param2 = "{{Parameters.tfp2}}"
  }
}

output "{{name}}" {
  value = local.{{name}}
}

